package com.example.demo.strategy;

import org.springframework.stereotype.Component;

@Component
public class WindSpeedJudge {

	public static boolean isMatch(double speed) {
		return (speed >= 8.0 && speed <= 10.7);
	}
}
