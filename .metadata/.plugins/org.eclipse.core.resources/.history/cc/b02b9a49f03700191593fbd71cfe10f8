package com.example.demo.strategy;

import org.springframework.stereotype.Component;

@Component
public class HumidJudge {

	public static boolean isMatch(int humid) {
		return (humid >= 40 && humid <= 80);
	}
}
